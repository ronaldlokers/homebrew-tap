# homebrew-tap

Homebrew formulae for [sugarrush](https://github.com/ronaldlokers/sugarrush) — a Nightscout CGM terminal UI.

```sh
brew install ronaldlokers/tap/sugarrush
```

The `sugarrush` formula is published automatically by the sugarrush release workflow (cargo-dist).
